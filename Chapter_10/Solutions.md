```
Q1:

What is the difference between shutil.copy() and shutil.copytree()?

Ans:shutil.cpoy() function is used to copy only one file wheras shutil.copytree will copy entire folder and everything inside it.
```
```
Q2:

What function is used to rename files?

Ans:shutil.move()
```
```
Q3:

What is the difference between the delete functions in the send2trash
and shutil modules?

Ans:shutil functions will permanently delete the files while send2trash will put them into recycle bin
```
```
Q4:

ZipFile objects have a close() method just like File objects’ close() method.
What ZipFile method is equivalent to File objects’ open() method?

Ans:zipfile.zipfile() is equivalent to ope() method>
```
