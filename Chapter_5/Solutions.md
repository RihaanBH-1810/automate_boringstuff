```
Quesiton 1:
What does the code for an empty dictionary look like?

Answer: ex_dict = {}
```
```
Question 2:

What does a dictionary value with a key 'foo' and a value 42 look like?

Answer:{'foo': 42}
```
```
Question 3:

What is the main difference between a dictionary and a list?

Answer:List is ordered and a dictionary is unordered ie. you cant acces dictionary values with index like in list.
```
```
question 4:

What happens if you try to access spam['foo'] if spam is {'bar': 100}?

Question: We will encounter a keyError 
```
```
Question 5:

If a dictionary is stored in spam, what is the difference between the
expressions 'cat' in spam and 'cat' in spam.keys()?

Answer:There is no differnce both will check if the key 'cat' exists in the dictionary spam or not and return True or 
False accordingly.
```
```
Question 6:

If a dictionary is stored in spam, what is the difference between the
expressions 'cat' in spam and 'cat' in spam.values()?

Answer: 'cat' in spam checks if the key 'cat' exists in the dictionary spam or not whereas,
'cat' in spam.values checks if the vale
```
```
Question 7:

What is a shortcut for the following code?
if 'color' not in spam:
spam['color'] = 'black'

Answer: spam['color'] = 'black'
```
```
Question 8:

What module and function can be used to “pretty print”
dictionary values?

Answer:pprint() and pformat().
```
